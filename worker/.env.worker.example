APP_URL=http://localhost:3000
INTERNAL_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=       # fallback image gen if not set per-channel
POLL_INTERVAL=10      # seconds
WORKER_CONCURRENCY=8  # set >= 2x max pipeline depth to avoid deadlocks
